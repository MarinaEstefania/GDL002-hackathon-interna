window.movies = {
     /*  filterGenere: (data, genere) => {
            const filteredType = data.filter(data => {
                  return data.Genre.indexOf(genere) > -1;
            });
            return filteredType;
      } */
    /*   filterGenere:  (data, type) =>{ 
            const arrayFiltered = data.filter((Tipo, index) => { 
              for (const i in data[index].Genre){
                if(data[index].Genre[i] === type) {
                  return data[index].Genre[i] === type;
                }
              }
            });
            return arrayFiltered;
          }, */
}
